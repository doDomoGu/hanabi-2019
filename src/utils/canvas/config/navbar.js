/* 导航栏绘制参数配置 */

import { dpr /*, width, height */ } from "../lib"

let _ = {}

_.bgColor = "#282c34"

/* 登出按钮 */
_.logoutBtn = {}
_.logoutBtn.textColor = "#ffffff"
_.logoutBtn.bgColor = "#ff4400"
_.logoutBtn.x = 20 * dpr
_.logoutBtn.y = 20 * dpr
_.logoutBtn.w = 800 * dpr
_.logoutBtn.h = 400 * dpr

export default _
